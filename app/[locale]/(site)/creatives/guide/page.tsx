import { setRequestLocale } from "next-intl/server";
import { resolveLocaleParam } from "@/lib/resolve-locale";
import { CreativesShell } from "@/components/creatives/creatives-shell";
import { CREATIVE_SPECS } from "@/lib/creatives/spec-guide"; export const dynamic = "force-static"; export default async function CreativeSpecGuidePage({ params,
}: { params: Promise<{ locale: string }>;
}) { const locale = await resolveLocaleParam(params); setRequestLocale(locale); const isKo = locale === "ko"; return ( <CreativesShell eyebrow={`[ ${isKo ? "소재 규격 가이드" : "Spec Guide"} ]`} title={ isKo ? ( <> 매체 유형별 <span className="tkad-home-accent-text">권장 규격</span> </> ) : ( <> <span className="tkad-home-accent-text">Recommended</span> Specs by Media Type </> ) } description={ isKo ? "DOOH 매체별로 가장 잘 보이는 해상도·비율·길이 가이드를 정리했습니다. 업로드 단계에서도 자동으로 안내합니다." : "Specs that work best on each DOOH surface. The upload step surfaces these hints automatically too." } isKo={isKo} > <div className="space-y-4"> <p className="rounded-xl border-2 border-border bg-muted/40 p-4 tkad-type-meta tracking-tight text-muted-foreground"> {`// `}{isKo ? "매체 상세 페이지에 '해상도' 가 표시돼 있다면 그 값이 가장 정확합니다. 이 표는 일반 권장값입니다." : "When a media detail page lists its own resolution, prefer that value over this general guide."} </p> <div className="overflow-x-auto rounded-2xl border-2 border-border bg-card"> <table className="w-full min-w-[44rem] border-collapse text-left text-sm"> <thead> <tr className="bg-muted"> <th className="border-b-2 border-border px-3 py-3 tkad-type-label text-muted-foreground"> {isKo ? "매체 유형" : "Media type"} </th> <th className="border-b-2 border-border px-3 py-3 tkad-type-label text-muted-foreground"> {isKo ? "권장 해상도" : "Resolution"} </th> <th className="border-b-2 border-border px-3 py-3 tkad-type-label text-muted-foreground"> {isKo ? "비율" : "Aspect"} </th> <th className="border-b-2 border-border px-3 py-3 tkad-type-label text-muted-foreground"> {isKo ? "포맷" : "Formats"} </th> <th className="border-b-2 border-border px-3 py-3 tkad-type-label text-muted-foreground"> {isKo ? "최대" : "Max"} </th> <th className="border-b-2 border-border px-3 py-3 tkad-type-label text-muted-foreground"> {isKo ? "영상 길이" : "Video length"} </th> </tr> </thead> <tbody> {CREATIVE_SPECS.map((row) => ( <tr key={row.type} className="odd:bg-card even:bg-muted/40"> <th className="border-b border-border/60 px-3 py-3 text-left align-top font-bold text-foreground"> {isKo ? row.labelKo : row.labelEn} </th> <td className="border-b border-border/60 px-3 py-3 align-top text-foreground/85"> {row.recommendedResolution} </td> <td className="border-b border-border/60 px-3 py-3 align-top tkad-type-meta text-foreground/85"> {row.aspectRatio} </td> <td className="border-b border-border/60 px-3 py-3 align-top tkad-type-meta text-foreground/85"> {row.stillFormats.join(" / ")} {row.videoFormats?.length ? ( <> <br /> <span className="text-muted-foreground"> {row.videoFormats.join(" / ")} </span> </> ) : null} </td> <td className="border-b border-border/60 px-3 py-3 align-top tabular-nums tkad-type-meta tabular-nums text-foreground/85"> {row.maxFileMB}MB </td> <td className="border-b border-border/60 px-3 py-3 align-top font-display tkad-type-meta tabular-nums text-foreground/85"> {row.videoDurationSec ? `${row.videoDurationSec.min}–${row.videoDurationSec.max}s` : "—"} </td> </tr> ))} </tbody> </table> </div> <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3"> {CREATIVE_SPECS.map((row) => ( <div key={row.type} className="rounded-2xl border-2 border-border bg-card p-4 sm:p-5" > <p className="tkad-type-label text-accent"> [ {isKo ? row.labelKo : row.labelEn} ] </p> <ul className="mt-3 space-y-1.5 tkad-type-meta leading-relaxed text-foreground/80"> {(isKo ? row.notesKo : row.notesEn).map((n) => ( <li key={n}>· {n}</li> ))} </ul> </div> ))} </div> </div> </CreativesShell> );
}
