import type { Metadata } from "next";
import { setRequestLocale } from "next-intl/server";
import { resolveLocaleParam } from "@/lib/resolve-locale";
import { buildShareMetadata, pageAlternates, siteKeywords } from "@/lib/seo";
import { fetchPublicMediaCatalog } from "@/lib/public-media-catalog";
import { buildPricingGuideStats, seoZoneKeywords,
} from "@/lib/pricing-guide-stats";
import { formatManWon } from "@/lib/media-price-transparency";
import { Link } from "@/i18n/navigation";
import { HomeLandingDayNight } from "@/components/home-landing-day-night";

export const dynamic = "force-dynamic";

type Props = { params: Promise<{ locale: string }> }; export async function generateMetadata({ params }: Props): Promise<Metadata> { const locale = await resolveLocaleParam(params); const isKo = locale === "ko"; const kw = seoZoneKeywords(); return { title: isKo ? "옥외광고 가격 가이드 · 지역별 단가 | THINKAD" : "OOH pricing guide · rates by district | THINKAD", description: isKo ? "강남 전광판 광고비, 지역별 평균 단가, 매체 유형별 가격 범위와 시즌·협상 팁을 공개합니다." : "Public OOH price ranges by media type and Seoul districts, with season and negotiation tips.", keywords: [ ...siteKeywords(locale), ...(isKo ? kw.ko : kw.en), ], alternates: pageAlternates(locale, "/pricing-guide"),
    ...buildShareMetadata({
      locale,
      title: isKo ? "옥외광고 가격 가이드 · 지역별 단가 | THINKAD" : "OOH pricing guide · rates by district | THINKAD",
      description: isKo ? "강남 전광판 광고비, 지역별 평균 단가, 매체 유형별 가격 범위와 시즌·협상 팁을 공개합니다." : "Public OOH price ranges by media type and Seoul districts, with season and negotiation tips.",
      path: "/pricing-guide",
      image: { kind: "segment", segment: "media" },
    }),
  };
} export default async function PricingGuidePage({ params }: Props) { const locale = await resolveLocaleParam(params); setRequestLocale(locale); const isKo = locale === "ko"; const catalog = await fetchPublicMediaCatalog(); const stats = buildPricingGuideStats(catalog); return ( <HomeLandingDayNight> <div className="mx-auto max-w-4xl px-4 py-12 pb-24"> <p className="font-display text-xs font-medium uppercase tracking-[0.24em] text-accent"> Pricing guide </p> <h1 className="mt-3 text-3xl font-black tracking-tight text-foreground sm:text-4xl"> {isKo ? "OOH 가격 가이드" : "OOH pricing guide"} </h1> <p className="mt-4 text-muted-foreground leading-relaxed"> {isKo ? `THINKAD에 등록된 ${stats.totalActive}개 활성 매체 데이터를 바탕으로 공개하는 참고 시세입니다. 실제 견적은 집행 기간·슬롯에 따라 달라질 수 있습니다.` : `Reference rates from ${stats.totalActive} active listings on THINKAD. Final quotes depend on flight dates and slots.`} </p> <section className="mt-10"> <h2 className="text-xl font-bold"> {isKo ? "매체 유형별 가격 범위 (월)" : "Price range by type (monthly)"} </h2> <div className="mt-4 overflow-x-auto rounded-xl border"> <table className="w-full text-left text-sm"> <thead className="bg-muted/50 text-xs text-muted-foreground"> <tr> <th className="px-4 py-2">{isKo ? "유형" : "Type"}</th> <th className="px-4 py-2">{isKo ? "최저" : "Min"}</th> <th className="px-4 py-2">{isKo ? "평균" : "Avg"}</th> <th className="px-4 py-2">{isKo ? "최고" : "Max"}</th> <th className="px-4 py-2">n</th> </tr> </thead> <tbody> {stats.typeRanges.map((r) => ( <tr key={r.type} className="border-t"> <td className="px-4 py-3 font-medium"> {isKo ? r.labelKo : r.labelEn} </td> <td className="px-4 py-3 tabular-nums tabular-nums"> {formatManWon(r.minWon, locale)} </td> <td className="px-4 py-3 tabular-nums tabular-nums"> {formatManWon(r.avgWon, locale)} </td> <td className="px-4 py-3 tabular-nums tabular-nums"> {formatManWon(r.maxWon, locale)} </td> <td className="px-4 py-3 text-muted-foreground">{r.count}</td> </tr> ))} </tbody> </table> </div> </section> <section className="mt-12"> <h2 className="text-xl font-bold"> {isKo ? "지역별 평균 단가" : "Average by district"} </h2> <ul className="mt-4 space-y-2"> {stats.zoneRows.map((z) => ( <li key={z.zoneId} className="flex flex-wrap justify-between gap-2 rounded-lg border px-4 py-3 text-sm" > <span className="font-medium"> {isKo ? z.labelKo : z.labelEn} </span> <span className="tabular-nums tabular-nums text-muted-foreground"> {formatManWon(z.minWon, locale)} ~ {formatManWon(z.maxWon, locale)} <span className="ml-2 text-foreground"> ({isKo ? "평균" : "avg"} {formatManWon(z.avgWon, locale)}) </span> </span> </li> ))} </ul> </section> <section className="mt-12"> <h2 className="text-xl font-bold"> {isKo ? "시즌별 가격 트렌드" : "Seasonal trends"} </h2> <ul className="mt-4 list-disc space-y-2 pl-5 text-sm text-muted-foreground"> {(isKo ? stats.seasonNotesKo : stats.seasonNotesEn).map((note) => ( <li key={note}>{note}</li> ))} </ul> </section> <section className="mt-12 rounded-2xl border-2 border-accent/30 bg-accent/5 p-6"> <h2 className="text-lg font-bold"> {isKo ? "협상 가능 폭" : "Negotiation room"} </h2> <p className="mt-2 text-sm leading-relaxed text-muted-foreground"> {isKo ? stats.negotiationKo : stats.negotiationEn} </p> <Link href="/media" className="mt-4 inline-block text-sm font-bold text-accent hover:underline" > {isKo ? "매체 둘러보기 →" : "Browse media →"} </Link> </section> </div> </HomeLandingDayNight> );
}
