"use client";

/**
 * ServicesFaq — 클라이언트 컴포넌트 (아코디언 토글).
 * chunk c 에서 본문 채움. 현재는 stub.
 */
export function ServicesFaq({ isKo }: { isKo: boolean }) {
  return (
    <p className="border-b-2 border-bx-black bg-bx-off px-6 py-16 text-center font-mono text-[11px] uppercase tracking-[0.22em] text-bx-gray-dim sm:px-10">
      // chunk c — FAQ TODO ({isKo ? "ko" : "en"})
    </p>
  );
}
