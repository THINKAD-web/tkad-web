import type { MediaPriceBenchmark } from "@/lib/media-price-transparency";
import { formatManWon } from "@/lib/media-price-transparency";
import { TrendingDown, TrendingUp } from "lucide-react";
import { RegionPriceAlertToggle } from "@/components/media-detail/region-price-alert-toggle"; export function MediaPriceTransparencyCard({ benchmark, locale,
}: { benchmark: MediaPriceBenchmark; locale: string;
}) { const isKo = locale.startsWith("ko"); const absPct = Math.abs(benchmark.percentVsAvg); return ( <section className="mt-6 rounded-2xl border-2 border-[color:var(--qp-accent)]/30 bg-[color:var(--qp-accent-soft)] p-5 sm:p-6" aria-labelledby="price-transparency-heading" > <p id="price-transparency-heading" className="tkad-type-label text-[color:var(--qp-accent)] dark:text-[color:var(--qp-accent)]/90" > {isKo ? "실시간 시세" : "Market rate"} </p> <h3 className="mt-2 text-[length:var(--qp-text-h3)] font-bold tracking-tight text-foreground"> {isKo ? "가격 투명성" : "Price transparency"} </h3> <ul className="mt-4 space-y-3 text-sm text-foreground"> <li className="flex flex-wrap items-baseline justify-between gap-2 border-b border-border/60 pb-3"> <span className="text-muted-foreground"> {isKo ? `${benchmark.zoneLabelKo} 평균 단가` : `${benchmark.zoneLabelEn} average`} </span> <span className="font-display font-bold tabular-nums"> {isKo ? "월 " : ""} {formatManWon(benchmark.regionAvgWon, locale)} </span> </li> <li className="flex flex-wrap items-baseline justify-between gap-2 border-b border-border/60 pb-3"> <span className="text-muted-foreground"> {isKo ? "이 매체" : "This media"} </span> <span className="flex items-center gap-2 font-display font-bold tabular-nums"> {formatManWon(benchmark.mediaPriceWon, locale)} {benchmark.cheaperThanAvg ? ( <span className="inline-flex items-center gap-1 rounded-md bg-emerald-500/15 px-2 py-0.5 tkad-type-title text-emerald-600 dark:text-emerald-300"> <TrendingDown className="h-3.5 w-3.5" aria-hidden /> {isKo ? `평균보다 ${absPct}% 저렴` : `${absPct}% below avg`} </span> ) : benchmark.percentVsAvg > 0 ? ( <span className="inline-flex items-center gap-1 rounded-md bg-amber-500/15 px-2 py-0.5 tkad-type-title text-amber-700 dark:text-amber-200"> <TrendingUp className="h-3.5 w-3.5" aria-hidden /> {isKo ? `평균보다 ${absPct}% 높음` : `${absPct}% above avg`} </span> ) : ( <span className="text-xs text-muted-foreground"> {isKo ? "평균 수준" : "Near average"} </span> )} </span> </li> <li className="flex flex-wrap items-baseline justify-between gap-2"> <span className="text-muted-foreground"> {isKo ? "유사 매체 시세 범위" : "Similar media range"} </span> <span className="tabular-nums tkad-type-title tabular-nums"> {formatManWon(benchmark.similarMinWon, locale)} {" ~ "} {formatManWon(benchmark.similarMaxWon, locale)} <span className="ml-1 text-xs font-normal text-muted-foreground"> ({benchmark.similarCount} {isKo ? "곳" : ""}) </span> </span> </li> </ul> <p className="mt-4 text-xs leading-relaxed text-muted-foreground"> {isKo ? `협상 가능 폭은 보통 표시가의 ${benchmark.negotiationHintPct}% 전후입니다. 「가격 제안하기」로 매체사에 익명 제안할 수 있습니다.` : `Negotiation room is often around ${benchmark.negotiationHintPct}% off list. Use “Propose a price” to reach the owner anonymously.`} </p> {benchmark.zoneId ? ( <RegionPriceAlertToggle regionZone={benchmark.zoneId} zoneLabel={isKo ? benchmark.zoneLabelKo : benchmark.zoneLabelEn} locale={locale} /> ) : null} </section> );
}
