/**
 * @deprecated Use inline layout in `app/[locale]/local/*` and `app/[locale]/type/*` pages.
 * Kept as a thin re-export target if needed later.
 */
export { SeoContextualLinks } from "@/components/seo/seo-contextual-links";
