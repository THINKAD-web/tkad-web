# THINKAD 다음 세션 인수인계 (2026-04-19/20)

> 이 문서는 다른 Claude Code 세션이 곧바로 작업을 이어받을 수 있도록 작성됐습니다.
> 작업 디렉터리: `/home/user/tkad-web` · 리포: `THINKAD-web/tkad-web`

---

## 1. 이번 세션에서 완료된 일 (3건 main 머지됨)

| PR | 커밋 | 내용 |
|---|---|---|
| #13 squash | `a34bba3` | **2026 리브랜딩 전체 적용** + media 빌드 오류 hotfix (18 파일) |
| #11 squash | `566fd3e` | **THINKAD Platform PRD v1** (`docs/THINKAD-PLATFORM-PRD.md`, 1,410줄) |
| #12 closed | — | 중복(내용 #13 에 포함됨)으로 close |

### 1-1. 리브랜딩 적용된 색상 (이미 main 반영됨)

| 용도 | 신규 hex |
|---|---|
| Primary Navy (딥 잉크) | `#0D1B2E` |
| Navy Light (hover) | `#1E3A5F` |
| Navy Dark (text) | `#0A1420` |
| Accent Gold (리치 앰버) | `#C8913C` |
| Gold Light | `#e0a85f` |
| Gold Dark | `#a67524` |
| 그라데이션 deep / deeper | `#070e18` / `#040914` |
| (유지) Silver / Page bg / CTA | `#B0B8C4` / `#D6D9E6` / `#9B3C31` |

핵심 파일: `tailwind.config.ts`, `app/globals.css`, `app/icon.tsx`, `app/apple-icon.tsx`, `components/campaign-report-preview.tsx`, `components/quote-pdf-preview.tsx` 등.

### 1-2. 빌드 오류 hotfix (이미 main 반영됨)
- `app/[locale]/media/page.tsx` — 중복 `export const revalidate` 제거 (force-dynamic + revalidate=0 페어만 유지)
- `components/media-detail-hero-gallery.tsx` — b33132a 에서 `... (파일 내용 전체)` 플레이스홀더로 손상된 것을 cfa5351 버전(115줄)으로 복원

---

## 2. 진행 중이었으나 **미완료** 인 작업

### 사용자가 요청한 PRD v2 (7섹션 통합본) — 시작만 함, 본문 없음
- 새 브랜치 `claude/prd-v2-hybrid-platform` 생성됨 (origin/main 기준)
- **파일 작성 안 됨** (사용자가 중단 요청)
- 사용자가 지정한 산출물 7개 (이 순서·이 구조 그대로 작성 필요):
  1. **제품 비전 & 포지셔닝** — 한 줄 포지셔닝, "전통 대행사 → 하이브리드 플랫폼" 전환 전략
  2. **상세 기능 명세서** (Phase 1–3 전부) — 각 기능 User Story + AC + Priority. 검증 배지·자동 제안서·VR 투어는 특히 상세히
  3. **사용자 플로우 & 화면 설계** — 주요 화면 6~8개 (홈→지도검색→플래너→제안서→My Dashboard→VR 등), 레이아웃·인터랙션, 모바일/PC 반응형
  4. **기술 아키텍처 & 스택 추천** — 권장 스택, AI 추천 엔진, PPT 자동 생성·VR·실시간 알림 구현 방식, 보안·스케일링·성능
  5. **개발 로드맵** — Phase 1–3 스프린트 분해, MVP 정의, FE/BE/디자인/PM 인력
  6. **차별화 전략 & OKR** — HOO 대비 압도 전략, 3/6/12개월 OKR
  7. **추가 제언** — 놓쳤을 가능성 있는 기능·리스크 3~5개, MVP 빠른 출시 방법

### v2 작성 시 참고할 입력 사항 (사용자 메시지 전문)
- Phase 1 (3개월 MVP): 지도검색 / 자동제안서 / 매체플래너 / 로그인+My
- Phase 2 (6개월 차별화): 유동인구 / 실시간 알림 / VR / 검증 배지
- Phase 3 (12개월 풀): 미디어 오너 포털 / 챗봇+전문가 하이브리드 / 다국어+PWA / 파트너 대행사
- 추가 차별화: Verified Media Only 필터, 풀서비스 vs 셀프서비스 선택제, 캠페인 성과 보고서 자동화
- 톤: "12년 경력 PM + Next.js 풀스택 + OOH 도메인 전문가"
- 사용자 인식: 현재 사이트(tkad-web.vercel.app)는 "고급 마케팅 랜딩" 수준이라고 보고 있음 — 즉 플랫폼 기능은 아직 공개되지 않은 상태로 인식. (실제로는 admin/planner/quote 등 부분 구현 존재 — 코드베이스 점검 필요)

### v2 작성 시 v1과 차별화 포인트
- v1 (`docs/THINKAD-PLATFORM-PRD.md`)은 4-Phase, 영문/기술 깊이 위주 — **참고용**
- v2는 **3-Phase 통합형**, 화면 설계 비중 ↑, OKR/스프린트 인력 산정 비중 ↑, 한국어 자연스러움
- v2 권장 파일 경로: `docs/THINKAD-HYBRID-PLATFORM-PRD-V2.md`

---

## 3. 코드베이스 핵심 사실 (반드시 숙지)

### 3-1. 시스템 환경
- Next.js **16.2.3** (App Router, locale `app/[locale]`)
- React 19.2.4, Tailwind 4.x, TypeScript 5
- Prisma 7.6 + PostgreSQL (`@prisma/adapter-pg`)
- next-intl 4.8.3 (KO/EN 활성, ZH/JP는 미구현)
- Anthropic SDK 0.88.0 (Claude API)
- Cloudinary 2.9 / Resend 4.8 / Nodemailer 8.0.5
- 지도 SDK는 npm 의존 없음 — Kakao Maps JS SDK CDN 직접 호출 (`lib/kakao-*.ts`)

### 3-2. AGENTS.md 절대 규칙
- "This is NOT the Next.js you know" — 코드 작성 전 `node_modules/next/dist/docs/` 확인 의무
- **`fetchPublicMediaCatalog` (DB-backed) 는 공개 매체 리스트/상세의 단일 진실 소스**. 목업/샘플/JSON-only 로 교체 금지

### 3-3. 디렉터리 구조
```
app/
  [locale]/               # KO/EN locale routes
    planner/              # 7-step wizard (현재 6단계 구현)
    quote/[id]/           # 셀프 견적 + 계약 e-서명
    media/[id]/           # 카탈로그 상세 (DB 기반)
    admin/(dashboard)/    # Admin 포털 (medias, networks, campaigns,
                          #   crm, quotes, quote-templates, inquiries,
                          #   media-hub, analytics, ai-content)
  api/                    # 89개 엔드포인트 (admin / public / cron)
components/               # 130+ React components
lib/                      # 130+ utilities (ai-*, build-*-pdf, kakao-*,
                          #   public-media-catalog, planner-*, crm-*)
prisma/schema.prisma      # 16 models (Media, MediaBooking, MediaNetwork,
                          #   CrmAccount, Campaign, OoHQuote, OohContract,
                          #   Quote, QuoteRequest, QuoteTemplate,
                          #   CaseStudy, TrendReport, AcademyLesson,
                          #   ContactInquiry, AdminUser, etc.)
docs/                     # PRD v1, deployment-checklist, e2e-test, ...
```

### 3-4. 기존 핵심 모델 (요약)
- `Media` ←→ `MediaNetworkLocation` ←→ `MediaNetwork`
- `Media` —(1:N)→ `MediaBooking`, `MediaPriceSnapshot`, `MediaAdvertiserExecution`
- `Campaign` —(1:N)→ `CampaignProofPhoto`, `CampaignScheduleEvent`, `CampaignFinancialDoc`
- `Campaign` —(1:1)→ `OoHQuote` —(1:1)→ `OohContract`
- `OoHQuoteStatus`: draft → sent → booking_requested → booking_pending → booking_confirmed → invoice_sent → payment_pending → payment_confirmed → contract_confirmed → in_progress → completed | cancelled

### 3-5. 인증 현황
- 일반 회원/광고주 인증 **없음** (현재는 익명 셀프서비스 + Admin 만)
- `AdminUser` 모델만 존재 (자체 세션, next-auth 미사용)
- Phase 1 MVP 의 "로그인 + My THINKAD" 는 신규 작업 — `User`, `UserSession`, OAuth 모두 추가 필요 (v1 PRD §6 참조)

---

## 4. 현재 Git 상태

```
Branch: claude/prd-v2-hybrid-platform  (origin/main 기준, 변경 없음)
원격: 푸시 안 됨

Main 최신 3개:
  566fd3e docs: add THINKAD platform PRD for HOO-surpassing hybrid (#11)
  a34bba3 refactor(brand): apply 2026 rebrand + restore media build (#13)
  b33132a feat(media): hero image uses 16:9 aspect ratio ...
```

---

## 5. 다음 세션이 해야 할 일 (우선순위 순)

### A. (필수) PRD v2 작성 완성
- 위 §2 의 7섹션 구조대로 `docs/THINKAD-HYBRID-PLATFORM-PRD-V2.md` 신규 작성
- v1 (`docs/THINKAD-PLATFORM-PRD.md`) 은 **참고만**, 복붙 금지 — 사용자가 v2 는 "3-Phase 통합형 + 화면 설계 + OKR" 강조한 새 구조를 원함
- 작성 팁: 1,500~2,500줄 분량 예상. 한 번에 다 쓰지 말고 4~5청크로 분할 Write/Edit (스트림 타임아웃 방지)
- 완성 후 commit → push → PR (브랜치명 그대로 `claude/prd-v2-hybrid-platform`)

### B. (선택) 사용자 요청 시 — Phase 1 MVP 실제 구현 착수
- 우선순위: ① 로그인+My THINKAD ② 지도 검색 ③ 매체플래너 고도화 ④ 자동 제안서 검증 배지 삽입
- v1 PRD `§6` Prisma 신규 모델·`§7` API 설계 그대로 사용 가능

### C. 사용자 환상에 대한 정직한 정정
- 사용자는 "랜딩 외에 플랫폼 기능 미구현"으로 인식 중
- 실제로는 `/planner`, `/quote`, `/admin`, `/compare`, `/recommend` 등이 이미 코드 존재
- 다만 **공개 노출 / 회원 시스템 / 지도 / 검증 배지 시각화 / VR** 등은 미구현
- 새 세션은 사이트 라이브 상태 점검 후 사용자에게 현 구현 범위를 정확히 보고 권장

---

## 6. 주의사항 / 함정

1. **스트림 타임아웃** — Opus 4.7 에서 긴 텍스트를 한 번에 출력하면 "API Error: Stream idle timeout" 발생. 반드시 `Write` 도구로 파일 직접 기록 + 청크 분할
2. **Bash 제약** — 시스템 프롬프트는 grep/cat/sed 직접 사용 자제 권고. 단 대량 hex 일괄 치환 같은 케이스는 sed 정당화 가능
3. **GitHub 권한** — `gh` CLI 없음, MCP `mcp__github__*` 도구 사용. 리포 스코프 `THINKAD-web/tkad-web` 으로 제한됨
4. **머지는 사용자 명시 승인 필수** — auto-merge 금지
5. **Vercel 프리뷰 빌드** — 프리뷰 배포가 fail 해도 GitHub Check 는 success 로 표시될 수 있음. 반드시 PR 코멘트의 Vercel bot 메시지로 실 배포 상태 확인
6. **`media-detail-hero-gallery.tsx`** — 이전에 손상된 이력 있음. 다시 변경 필요시 cfa5351 버전 보존 주의
7. **next-intl** — 번역 키는 `messages/ko.json`, `messages/en.json` 에 있음. 신규 컴포넌트는 양쪽 다 추가
8. **AGENTS.md** 의 한글 규칙: "목업·샘플 데이터로 교체하지 말 것" — Phase 2 자동화 작업 시 특히 위반하기 쉬움

---

## 7. 한 줄 요약 (다음 세션이 첫 응답으로 사용 가능)

> "이전 세션에서 2026 리브랜딩(딥잉크/리치앰버) 전체 적용 + 빌드 오류 hotfix + Platform PRD v1 (1,410줄) 머지 완료(#11/#12/#13). 지금 요청하신 7섹션 통합 PRD v2 는 브랜치 `claude/prd-v2-hybrid-platform` 만 만들어진 상태이므로, 본문 작성부터 시작합니다."
