# 로컬 환경 변수 (사이드바 바로가기)

이 파일을 펼치면 옆에 `.env.local` 이 보이도록 설정해 두었습니다.

- 편집할 파일: **`.env.local`** (프로젝트 루트, Git에 올라가지 않음)
- 처음 만들 때: `cp .env.production.example .env.local`
- 예시·체크리스트: `.env.production.example`

탐색기에 여전히 안 보이면: **View → Command Palette → "Reload Window"**
